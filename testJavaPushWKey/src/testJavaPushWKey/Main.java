package testJavaPushWKey;

public class Main {

	public static void main(String[] args) {
		//Note: As the method of the class HelloWorld is static, 
		//it does not require an instance to be executed.
		//Furthermore, if it is instantiated and the method is called
		//using instance reference, a warning is generated stating that
		//a static method should be accessed in a static way
		HelloWorld.main(args);
	}

}
